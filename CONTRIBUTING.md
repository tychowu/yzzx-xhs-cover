# 团队协作：直接更新 main

有写入权限的成员直接在 main 提交、推送，不新建分支、不创建 PR、不等待负责人或其他用户审批。陌生用户不会因此获得写入权限。

## 同事复制这句话给 AI

> 我要改进并推送这个 Skill：……。请检查官方最新版，保留本地修改，同步最新 main，直接修改、测试和推送 main，不新建分支、不创建 PR。不上传生成图片、人物控制图、客户资料或密钥。版本号递增，在更新记录及提交说明中写清改动、原因、影响、实际测试结果和风险。出现冲突先说明，不覆盖别人的修改、不强推。

只反馈、不想自己修改的同事，可直接在仓库 Issues 中描述问题；不必获得写入权限。需要提交修改的同事使用各自 GitHub 账号，由负责人逐个邀请，绝不共享账号或令牌。

## 提交与发布

1. 检查工作区、实际分支和官方 main。干净且可快进时同步；若在旧分支，先检查独有提交与未提交修改，不能丢弃或擅自整支合入。无法安全同步时暂停说明，不新建分支绕过问题。
2. 运行仓库检查及相关测试，检查无关改动和隐私资料；视觉测试仍需明确授权。只暂存本次改动，根据暂存 diff 自动填写提交标题和正文：原因、改动、影响、实际测试结果、未测试部分和风险，不虚构通过结果。
3. 用户授权提交推送后直接推送 main，不创建 PR、不请求审批。仅修改或本地提交不授权推送。并发更新导致拒绝推送时重新获取远程，保留双方内容，重新核对版本号和测试；冲突暂停，不强推，不复用已有标签。
4. main 推送后的自动检查通过后创建 Git 标签与 Release；使用者在标题确认后统一对比官方 main，先确认再更新当前全局或项目副本，不自动跨目录同步。
5. 推送后报告提交链接、自动检查及发布的真实状态。检查失败不等于推送失败；发现问题用新的修复提交恢复稳定内容，版本号继续增加，不删除历史。普通生图不自动提交发布。

main 取消 PR 审批、CODEOWNERS 审批及推送前必过检查门槛，保留禁止强推和删除 main。自动检查仍在推送后运行，成功后才由既有流程发布 Release。自动提交说明是 AI 使用本 Skill 时执行的流程，不是任意 Git 客户端的后台钩子。

## 自动检查的边界

检查风格 JSON、学习素材路径、Markdown 相对链接、版本号与更新记录，拦截常见输出目录、私人素材文件名和明显密钥模式。只能帮助发现问题，不能保证识别所有隐私、秘密或画面质量；提交者仍须检查变更清单。允许入库的学习参考与确认示意图也必须有分享权。
