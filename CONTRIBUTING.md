# 团队只需记住：提修改，由负责人发布

`main` 是唯一正式版。每次改进建一个临时草稿分支，测试后提交更新申请（Pull Request）；不要直接推送 main。分支命名由 AI 处理，合入后自动删除。

## 同事复制这句话给 AI

> 我要改进这个 Skill：……。请检查官方最新版，保留本地修改，基于最新 main 建立独立草稿分支，一次只改这一件事。测试后提交更新申请给 tychowu，不直接合并，不上传生成图片、人物控制图、客户资料或密钥。版本号末位加一，并在更新记录里写清日期、改动和测试结果。出现冲突先说明，不覆盖别人的修改。

只反馈、不想自己修改的同事，可直接在仓库 Issues 中描述问题；不必获得写入权限。需要提交修改的同事使用各自 GitHub 账号，由负责人逐个邀请，绝不共享账号或令牌。

## 负责人审核与发布

1. 查看申请的“问题、修改、测试”三项。让 AI 检查无关改动和隐私资料，视觉调整试生成一张（明确授权后）。
2. 自动检查通过后确认合入。团队申请需要你的 CODEOWNERS 审核；批准后使用 Squash and merge，保持一次修改一条正式记录。
3. 两个申请版本号相同时，后合入者先基于最新 main 更新版本号和记录，重新检查。一个版本对应一个正式提交，不覆盖已有标签。
4. 合入 main 后自动创建同版本 Git 标签和 GitHub Release；团队下次使用检测到新版正式 Release 时自动安全同步全局 Skill。普通 pull 只更新项目副本，不把未发布的 main 或草稿内容同步到全局；本地有修改时暂停。
5. 发现问题，提交恢复稳定内容的修复申请，版本号继续增加；不要删除历史、强推或复用旧版本标签。

管理员保留应急绕过权限，用于首次启用配置及本人提交无法自我批准的情况；日常仍先看自动检查与测试结果，不替团队绕过审核。GitHub 管理员可以更改保护规则，这不是不可绕过的安全边界。

## 自动检查的边界

检查风格 JSON、学习素材路径、Markdown 相对链接、版本号与更新记录，拦截常见输出目录、私人素材文件名和明显密钥模式。只能帮助发现问题，不能保证识别所有隐私、秘密或画面质量；负责人仍要看变更清单。允许入库的学习参考与确认示意图也必须有分享权。
