---
name: YZZX-cover
description: 言直在线的专属视频封面生成skill。适用于ip直接根据图像套用模板生成封面图，或根据文案内容自行生成封面。默认输出 4 套真正不同的方案，并在支持图片生成时一次交付 4 张独立封面供比较。
agent_created: true
---

# 小红书封面生成 Skill

## 模型偏好

在 WorkBuddy 中调用本 skill 时，优先使用 hy4 大模型进行封面分析与生成。

把用户的笔记或视频内容，转化为可执行、可比较、可验收的小红书封面方案；在环境支持图片生成或设计工具时，继续生成最终封面。

## 核心原则

1. 一张封面最好让目标用户在一秒内得到三个答案：
   - 和我有关吗？
   - 对我有什么用？
   - 为什么值得相信？
2. 信息可以有三个，但视觉中心只能有一个。
3. 主标题负责抓住注意力；副标题负责说明收益；角标负责提供真实可信的证据。
4. 可信证据不是必填项。没有真实证据时，宁可不写，也不要编造“官方”“实测”“第一”“最新”等标签。
5. 先解决信息结构，再考虑装饰和风格。
6. “4 套方案”默认对应“4 张独立成图”，不能只生成 1 张，也不要把 4 张拼成一张四宫格。

## 工作流程

### 第一步：读取输入

优先从用户提供的笔记主题、逐字稿、正文、图片或已有封面中提炼：

- 笔记主题
- 笔记内容信息
- 目标人群
- 核心价值
- 可信证据
- 必须保留的文字、人物、产品、品牌元素
- 用户是否已直接提供主标题、副标题（如有，原样记录，第二步据此结合逐字稿校验，不擅自改写）
- 用户指定的尺寸、风格、颜色或禁用项

用户上传参考图，或明确引用此前使用过的封面风格时，先读取并查看参考图，再提炼人物、情绪、字体、配色、构图和质感；没有实际看到参考图时，不要仅凭文字印象声称已经复刻。

信息足够时直接执行，不要为了形式而提问。

信息不足且会明显影响结果时，一次最多提出 4 个简短问题：

1. 这篇内容具体讲什么？
2. 最希望哪类人点击？
3. 用户看完能得到什么最明确的收益？
4. 有哪些真实的可信证据可以放在封面上？

如果用户只提供了正文或逐字稿，先自行推断目标人群、核心价值和可能的可信证据，并清楚标注推断；不要重复询问正文中已经给出的信息。

### 第二步：提炼封面信息

先确定唯一的核心传播句，再分别生成：

- **主标题**：说明主题，同时提供相关性、冲突、结果、问题或好奇心中的至少一种。
- **副标题**：说明用户能获得的具体价值、方法、步骤、结果或适用场景。
- **角标/可信标签**：只使用真实证据，例如官方资料、真实数据、亲自测试、明确身份、案例结果或内容页数。

默认字数规则：

- 主标题优先控制在 8–14 个中文字符，通常不超过 16 个；必要时可分成两行。
- 副标题优先控制在 8–18 个中文字符。
- 角标优先控制在 2–8 个中文字符。
- 同一层级不要同时表达两个核心主题。

**分支处理：用户是否已提供主标题和副标题**

- 若用户在第一步已提供主标题和副标题：以提供的逐字稿为唯一依据，对照上方「核心原则」逐条校验这两组文案——重点看主标题是否抓住注意力（相关性 / 冲突 / 结果 / 问题 / 好奇心中至少一种）、副标题是否清楚说明用户收益、整张封面是否只有一个视觉中心、是否未伪造权威证据、缩略图下是否仍清晰。全部满足则沿用用户原文，不擅自改写。若存在不满足，明确指出违背了哪条核心原则，并在保留用户原意的前提下给出主标题、副标题的修改建议（只补强缺失的钩子或收益表述）；可一并附「原文版」和「建议版」。
- 若用户未提供主标题和副标题：按上述默认规则，从逐字稿中提炼生成主标题、副标题与角标。

### 第三步：输出 4 套差异化方案

每次默认输出 4 套方向。四套方案必须在以下至少两个维度上真正不同，而不是只替换几个同义词：

- 点击钩子：反常识、结果收益、步骤教程、权威证据、痛点问题、对比冲突
- 信息结构：主标题优先、副标题优先、数字优先、证据优先
- 版式：居中海报、上下分区、左右分栏、卡片式信息、人物与文字组合
- 视觉气质：极简、知识感、科技感、强冲击、轻松生活化、专业报告感

每套方案包含：

1. 方向名称
2. 适合的目标人群或点击动机
3. 主标题
4. 副标题
5. 角标/可信标签；没有真实证据时写“无”
6. 信息层级
7. 版式说明
8. 画面与视觉元素建议
9. 为什么这套方案可能有效

输出后推荐其中 1 套，并说明推荐理由。不要默认四套都一样好。

用户未指定视觉风格且需要 4 套方案或 4 张成图时，优先从 `references/styles/` 中已学习的风格里选取：

- 已学习过风格：尽量覆盖不同的风格，使 4 套在版式、配色或视觉气质上真正不同；每套的标题、角度和具体道具仍从内容推导。
- 尚未学习任何风格：不套用任何内置固定风格。提示用户二选一——指定一种风格，或运行「风格学习工作流」先教给 Skill 1–N 种风格，再生成。
- 用户明确指定其他组合、选定单一方向或只要 1 张时，以用户要求为准。

### 第四步：生成最终封面

- 用户只选定 1 个方向或明确只要 1 张时，如当前环境支持图片生成或设计工具，直接生成该方向的最终封面。
- 用户明确说“直接生成”“你来决定”或未限定数量时，默认将 4 套方案分别生成 1 张，共一次交付 4 张独立封面，不再重复确认。
- 默认四张应分别采用 references/styles/ 中已学习的不同风格，保证四张在版式、配色或视觉气质上明显不同；尚无已学风格且用户未指定时，先按「第三步」引导用户学习或指定风格，不要凭空套用固定视觉方向。
- 四张成图至少在点击钩子、人物情绪、信息结构、版式或视觉气质中的两个维度存在明显差异；不得只换颜色、背景或少量装饰。
- 四张必须分别输出，便于用户逐张查看、保存和比较；不要合并成四宫格。
- 用户只需要文案或方案时，不强制生成图片。
- 当前环境不支持图片生成时，输出可直接交给设计师或图片模型使用的制作说明，包括画布比例（3:4，像素 1152×1536）、文字、层级、构图、背景、字体气质、素材和禁用项。
- 生成图片后必须执行「交付前图像验收」；未通过不得展示、写入项目或称为最终成图。发现问题时优先修正文案、版式或人物一致性，而不是继续增加装饰。

### 交付前图像验收（所有成图必做）

逐张放大检查，并以用户确认的文字和人物参考图为唯一依据：

1. **文字结构与字形**：主标题、副标题、标签及所有必须出现的数字、中文、英文必须逐字完整；不得缺字、重复、错别字、乱码、残缺笔画、断裂字形、拉伸变形或被裁切。没有用户确认的文字时，画面不得出现可读的伪文字。
2. **文字排版逻辑**：同一信息层级使用一致的字体气质、字重、颜色和对齐逻辑；标题与副标题的阅读顺序清楚，行距、字距和换行自然；文字不与人物脸部、手势、关键道具或画面边缘发生无意碰撞。主标题仍为唯一第一视觉中心。
3. **人物五官与身份**：只要用户提供人物参考图，必须逐项对照脸型、眉眼、鼻形、嘴部、发际线/发型、眼镜、胡须等稳定特征；人物身份不得被重绘为相似陌生人，不得美化到改变五官比例，不得替换、删除或新增眼镜、胡须等身份特征。用户未授权时，不得修改人物的服装版型和关键外观。
4. **画面完整性**：检查手指、手臂、衣物、纸张、道具和透视是否自然；不得有多余肢体、破损物体、边缘融化、拼接痕迹、水印或 logo。

任一项不通过时，只针对失败项重新生成或修正，然后从第 1 项重新验收；最多迭代 3 次。第 3 次仍无法满足人物身份或文字准确性时，说明具体失败项，不交付为最终图。

### 第五步：验收

逐项检查：

1. 缩小到手机缩略图后，主标题是否仍然清楚？
2. 一秒钟内，能否知道它讲什么、有什么用、为什么可信？
3. 整张封面是否只有一个视觉中心？
4. 主标题、副标题和角标的层级是否清楚？
5. 可信标签是否真实，有没有夸大或伪造权威？
6. 是否存在可以删除而不影响理解的文字、图标、边框或装饰？
7. 文案是否与笔记正文一致，没有“封面承诺大于内容交付”？
8. 中文、数字、英文大小写和专有名词是否准确？
9. 四张成图是否都已交付，并且不是同一构图的轻微变体？
10. 用户未指定风格时，四张是否分别采用了 references/styles/ 中已学习的不同风格，或已引导用户先学习/指定风格？

任意一项不通过，先做减法，再重新验收。

## 默认输出格式

按 [references/output-template.md](references/output-template.md) 的格式输出。

需要更详细的标题、信息层级、视觉布局和可信证据规则时，读取 [references/cover-design-sop.md](references/cover-design-sop.md)。

## 迭代与经验回写

用户反馈某个结果不好时：

1. 先修改当前方案。
2. 判断反馈是否能抽象成以后都适用的规则。
3. 将可复用经验写成明确、可执行、可验收的句子，例如：
   - 不写“标题短一点”，改成“主标题默认不超过 16 个中文字符；超过时先压缩再生成”。
   - 不写“画面简单一点”，改成“封面只保留一个视觉中心；删除不影响理解的装饰元素”。
4. 只有在用户明确要求更新 Skill，且当前环境允许编辑 Skill 文件时，才修改本 Skill；否则输出建议新增的规则文本。

## 禁止事项

- 不编造官方背书、数据、测试结果、页数、身份或案例。
- 不把四套方案做成同一文案的轻微改写。
- 不把“一次生成 4 张”降级成只给 4 套文案、只生成 1 张，或输出一张四宫格。
- 不让主标题、副标题、角标和装饰同时抢夺注意力。
- 不为了追求“高级感”牺牲缩略图可读性。
- 不使用与正文无关的夸张承诺诱导点击。
- 不在信息已经足够时反复追问用户。

## 通用生图规则（适用于所有风格、所有出图）

以下规则对所有封面生成生效，不局限于某一个风格：

- 画布比例强制为 3:4（宽:高 = 0.75）。实际像素使用 1080×1440 ；**禁止**输出 1024×1536（那是 2:3，画面会被纵向拉长变形），也禁止任何非 3:4 比例。所有 ImageGen 调用或设计工具导出均按 3:4 执行，不可拉长。
- 字体多样性：标题字体根据图像内容自动推荐，可混合粗黑体、手写体、变体/装饰体、竖排等，保留原参考图的手写体、变体风格特征。

## 风格学习工作流

让用户把喜欢的封面图「教」给 Skill，提取成可复用的风格模板，存入 references/styles/，并登记到 references/styles/README.md。

### Phase 1：上传与分析

请用户上传 1–5 张参考图（支持 PNG / JPG / WebP）。

- 0 张：提示「至少需要 1 张参考图」。
- 超过 5 张：选取视觉差异最大的 5 张，并说明筛选原因。

用 Read 工具读取每张图片，分析以下维度：

- 配色：主色、辅色、点缀色（尽量给出 hex 值）、整体色彩情绪。
- 字体感：粗细（轻 / 常规 / 粗）、风格（无衬线 / 衬线 / 手写 / 装饰）、大小层级。
- 构图：文字与人物的位置关系、文字占比、留白多少、对齐方式。
- 装饰元素：有无边框、贴纸、图标、几何图形、背景纹理。
- 整体氛围：用 2–3 个关键词概括（如「温暖、治愈、ins 风」）。

若多张图风格冲突，明确指出冲突点，请用户选择方向，而不是取平均。

向用户展示分析结果（用自然语言，不要直接甩 JSON）：

> 我从你的图片中提取了这些风格特征：  
> 配色：暖奶油底色，深棕文字，粉色点缀  
> 字体：标题超大粗体，副标题细小，对比强烈  
> 构图：人物居中，标题压在头顶，底部色块横幅  
> 氛围：温暖、生活感、小红书爆款风  
> 你觉得这些对吗？有没有想调整的地方？

### Phase 2：确认与生成 Prompt

用户确认分析结果（允许最多 3 轮调整）：

- 「颜色再亮一点」→ 调整配色描述。
- 「想要更简约」→ 减少装饰元素。
- 「字体感觉不对」→ 进一步询问偏好。

请用户为这个风格起个名字（建议基于氛围关键词，如「暖橙励志」）。

根据分析结果，生成对应的风格 prompt，格式与风格库 JSON 一致：

```json
{
  "name": "用户起的中文名",
  "prompt": "根据分析结果撰写的图片生成提示词..."
}
```

Prompt 撰写要点：

- 明确【文字区域划分】（主标题在哪、副标题在哪）。
- 明确【字体风格】（参考分析结果翻译成生图描述语言）。
- 明确【背景 / 场景】。
- 结尾加【禁止事项】（禁止多余文字、禁止修改人脸、禁止非 3:4、禁止标签与色块重叠）。
- 系列标签等可变文案：生成前必须强制以对话框形式询问用户，并注明「推荐 ip 名称」；不得写死固定文字（详见「通用生图规则」）。

把生成的 JSON 保存到 references/styles/ 目录，文件名用英文、与风格库已有风格格式一致，例如 references/styles/warm-orange-motivation.json（用 Write 工具写入）。

同时把用户选中的一张参考图复制到 references/styles/，用风格英文名命名（如 warm-orange-motivation.jpg）。

更新 references/styles/README.md，登记该风格的名称、JSON 文件、参考图片与 prompt 摘要。

确认：「✅ 风格『{name}』已保存！下次生成时直接选择这个风格就行。」

### Phase 3：测试生成

询问用户：「要不要用这个风格试生成一张看看效果？」

若用户同意，收集测试内容（或使用默认标题「5 个让生活变好的小习惯」），并先确认系列标签文案（未指定则强制询问，注明「推荐 ip 名称」）。

优先用本 skill 自带去水印脚本生成（请求体固定 `LogoAdd=0`，需运行态 `BUDDY_CLOUD_TOKEN`）：

```bash
BUDDY_CLOUD_TOKEN=<token> python3 ${SKILL_DIR}/scripts/hunyuan_img_no_logo.py \
  image "组装好的提示词" --resolution 1152:1536 --output-dir <dir>
```

若需带参考角色图（图生图）或环境无 token，降级使用 ImageGen 工具；注意 ImageGen 参数未暴露 `LogoAdd`，生成图可能仍带右下角水印。可先用 `node ${SKILL_DIR}/scripts/generate.mjs --style <id> --title <标题>` 组装最终 prompt（仅输出文本，不出图）再交给生图脚本。

用 Read 工具展示生成结果，和原参考图放在一起让用户对比。

若不满意：找出哪个维度有偏差，回到 Phase 2 修改 prompt，最多迭代 3 次。

3 次后仍不满意：「可以先保存当前版本，之后随时再优化」。

### 错误处理

- 参考图分辨率过低（短边 < 500px）：提醒用户分析精度可能下降，建议换高清图。
- 参考图不是封面设计（纯照片、无排版元素）：说明此工作流需要有文字设计的封面图，纯人像照适合直接去生成封面。
- 用户中途放弃：告知当前进度已到哪一步，风格文件是否已保存，下次可以从哪里继续。
