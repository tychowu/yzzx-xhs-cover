---
name: yzzx-cover
description: 言直在线的专属视频封面生成skill。适用于ip直接根据图像套用模板生成封面图，或根据文案内容自行生成封面。先收集文案与人物照片，确认主副标题，再推荐 4 种风格并打开风格站供用户选择；确认后按所选风格生成独立封面。
metadata:
  agent_created: true
---

# 小红书封面生成 Skill

把用户的笔记或视频内容，转化为可执行、可比较、可验收的小红书封面方案；在环境支持图片生成或设计工具时，继续生成最终封面。

## 核心原则

1. 一张封面最好让目标用户在一秒内得到三个答案：
   - 和我有关吗？
   - 对我有什么用？
   - 为什么值得相信？
2. 信息可以有三个，但视觉中心只能有一个。
3. 主标题负责抓住注意力；副标题负责说明收益；角标负责提供真实可信的证据。
4. 可信证据不是必填项。没有真实证据时，宁可不写，也不要编造“官方”“实测”“第一”“最新”等标签。
5. 先解决信息结构，再考虑装饰和风格。
6. “4 套方案”默认对应“4 张独立成图”，不能只生成 1 张，也不要把 4 张拼成一张四宫格。

## 首次运行：检查并安装环境

新电脑首次使用、迁移安装或依赖报错时，在处理封面前完成本节。用户安装并使用本技能时，默认执行缺失依赖安装，不只给安装建议；遵守宿主系统的权限机制。后续运行仍做轻量检查，不能仅凭“已初始化”标记跳过损坏检测。

1. 定位当前技能完整目录，检查 `SKILL.md`、`references/styles/`、输出模板及所需脚本。按实际 JSON 数量报告风格数；只复制入口文件不算完整安装。若完整项目库可用，优先使用完整项目库。
2. 检查可用 Python 3（Windows 可用 `py -3`）。没有时优先使用宿主提供的 Python；否则通过已有包管理器安装 Python 3：macOS Homebrew 用 `brew install python`，Windows winget 用 `winget install -e --id Python.Python.3.12`，Debian/Ubuntu 用 `sudo apt-get install python3 python3-venv python3-pip`。只执行当前系统适用的命令；没有包管理器时使用 Python 官方安装渠道。遇权限或网络阻塞，说明具体原因，不声称已安装。
3. 运行 `python3 <技能目录>/scripts/bootstrap.py --install`。它在用户缓存目录创建独立虚拟环境并安装 PyYAML，不依赖旧电脑的系统包。按输出的 `python` 路径运行后续 Python 脚本及宿主提供的 `quick_validate.py`；不要装到一个解释器却用另一个运行。可用 `YZZX_RUNTIME_DIR` 指定可写的环境目录。若缺少 venv/ensurepip，先通过当前系统的 Python 安装渠道补齐后重试。
4. 按实际使用的生成路径补齐依赖：
   - 宿主内置图片生成与浏览器工具：检查工具可调用，无需安装 OpenAI SDK、Node.js 或获取 API key。推荐优先使用此路径。
   - `generate.mjs` 提示词脚本：运行 `bootstrap.py --install --node`。缺少 Node.js 18+ 时，通过已有包管理器安装受支持 LTS（如 `brew install node` / `winget install -e --id OpenJS.NodeJS.LTS`），再复查。此脚本提示词模式无 npm 依赖，确保不设置 `IMAGE_PROVIDER`。其旧版直连 API 路径不属于环境检查，不自动调用、收费或用它测试生图。
   - `_measure_color_area.mjs` 是可选开发辅助脚本，不属于封面必需环境；使用前先把历史绝对素材路径改为本次实际路径，并在技能目录安装 `jpeg-js`（`npm install --no-save --package-lock=false --ignore-scripts jpeg-js@0.4.4`）。普通出图不安装它的依赖。
5. 安装后重新运行相同检查，确认导入成功、风格库可读和所需工具可用。环境检查不生图、不上传照片、不消耗生成额度。只有检查成功才报告环境就绪；失败时保留具体错误，修复后再检查，不无限重试。

## 工作流程

### 第一步：收集文案与人物照片

首次开始制作时，请用户提供文案（正文或逐字稿）和人物照片；如有选定的主标题、副标题，也可一并提供。建议提示：

> 请上传这期的文案和人物照片；如果已经选定主标题、副标题，也可以一起发来。

已提供的材料不重复索取，只补齐缺失项。用户明确不需要人物时可省略照片；只有文案时可先分析标题，人物风格推荐等收到照片后再完成。必须实际查看照片，依据人物外观、服装、表情和姿态判断适配风格，不凭空推测身份。

优先读取当前项目的风格库及索引，并核对实际 JSON 文件；若安装目录与项目版本不同，说明差异并使用当前项目的完整库，不把旧安装副本的数量当作全部风格。

### 输入分析

优先从用户提供的笔记主题、逐字稿、正文、图片或已有封面中提炼：

- 笔记主题
- 笔记内容信息
- 目标人群
- 核心价值
- 可信证据
- 必须保留的文字、人物、产品、品牌元素
- 用户是否已直接提供主标题、副标题（如有，原样记录，第二步据此结合逐字稿校验，不擅自改写）
- 用户指定的尺寸、风格、颜色或禁用项

用户上传参考图，或明确引用此前使用过的封面风格时，先读取并查看参考图，再提炼人物、情绪、字体、配色、构图和质感；没有实际看到参考图时，不要仅凭文字印象声称已经复刻。

信息足够时直接进入文案分析；仍需完成下述标题确认与风格确认。

信息不足且会明显影响结果时，一次最多提出 4 个简短问题：

1. 这篇内容具体讲什么？
2. 最希望哪类人点击？
3. 用户看完能得到什么最明确的收益？
4. 有哪些真实的可信证据可以放在封面上？

如果用户只提供了正文或逐字稿，先自行推断目标人群、核心价值和可能的可信证据，并清楚标注推断；不要重复询问正文中已经给出的信息。

### 第二步：提炼封面信息

先确定唯一的核心传播句，再分别生成：

- **主标题**：说明主题，同时提供相关性、冲突、结果、问题或好奇心中的至少一种。
- **副标题**：说明用户能获得的具体价值、方法、步骤、结果或适用场景。
- **角标/可信标签**：只使用真实证据，例如官方资料、真实数据、亲自测试、明确身份、案例结果或内容页数。

默认字数规则：

- 主标题优先控制在 6–14 个中文字符，通常不超过 16 个；必要时可分成两行。
- **奶油巨字风格例外**：主标题必须恰好为 4 个中文字符，优先于默认字数规则。若在风格选择阶段选中奶油巨字，而已确认标题不是 4 个字，先依据原文案提出 4 字标题并请用户确认，不能自行删改已确认文字后直接生图；其他风格保留各自已确认的标题。
- 副标题优先控制在 8–18 个中文字符。
- 角标优先控制在 2–8 个中文字符。
- 同一层级不要同时表达两个核心主题。

**提出建议并确认标题**

- 从文案提炼主题、目标人群、核心收益和真实证据，提出你认为更好的主标题与副标题，简述推荐理由。不得让标题承诺超出正文。
- 用户提供了标题时，保留原文供对照，给出建议版并说明改善点；原文已最合适时可推荐保留，不为改写而改写。建议不等于已获准替换。
- 明确请用户确认使用原文版、建议版，或提出修改。等待明确答复，再进入风格选择；确认后的文字不得因换风格擅自改写，需要调整时再次确认。

### 第三步：推荐风格、打开站点并确认

1. 读取风格库索引、候选 JSON 和参考图，结合文案内容、已确认标题及人物照片，推荐 4 种已收录风格。每种列出准确风格名、内容与人物的适配理由，以及拟采用的构图、字体和配色。四种在版式、信息结构或视觉气质中至少两个维度明显不同；同一组已确认标题可以用于全部风格。
2. 标出最推荐的一种并解释原因。若用户已明确选定风格，以其选择为准，不强迫接受四种推荐；库中不足四种时如实说明。
3. 使用可用的浏览器打开工具，为用户打开 [风格展示站](https://yzzxref.diwu-pru.vip/)。在 Codex 中可用 `mcp__codex_app__open_in_codex` 打开该 URL 的浏览器面板；其他环境使用相应浏览器工具。不能打开时如实说明并提供可点击链接，不声称已打开。
4. 打开后请求用户确认推荐组合，或从站点选择其他一种/多种风格。可提示：“你可以采用这 4 种推荐，也可以在站点选择其他一种或多种风格，请告诉我风格名称。”等待明确选择后才生图，不能把打开网页或未回复当作确认。
5. 默认每种选定风格生成 1 张独立封面：接受四种推荐即生成 4 张；选择一种即生成 1 张；选择多种即按所选数量生成。用户明确指定其他数量时遵从其要求。站点所选风格若本地没有规则，先核实对应资源，不冒用其他风格代替。

标题确认和风格确认是两个依次进行的步骤。“生成封面”或未限定数量不代表跳过确认；用户在当前对话明确确认过的同一内容不重复询问。

### 第四步：生成最终封面

- 主副标题与风格都明确确认后，按每种所选风格 1 张生成独立封面；数量按第三步或用户明确要求执行，不再额外要求一次生图确认。
- 多张成图在人物情绪、信息结构、版式或视觉气质中至少两个维度明显不同；保持已确认文案不变，不得只换颜色、背景或少量装饰。
- 每张分别输出，便于逐张查看、保存和比较，不合并成四宫格。
- 用户只需要文案或方案时，不强制生成图片。
- 当前环境不支持图片生成时，输出可直接交给设计师或图片模型使用的制作说明，包括画布比例（3:4，像素 1080×1440）、文字、层级、构图、背景、字体气质、素材和禁用项。
- 生成图片后必须执行「交付前图像验收」；未通过不得展示、写入项目或称为最终成图。发现问题时优先修正文案、版式或人物一致性，而不是继续增加装饰。

### 交付前图像验收（所有成图必做）

逐张放大检查，并以用户确认的文字和人物参考图为唯一依据：

1. **文字结构与字形**：主标题、副标题、标签及所有必须出现的数字、中文、英文必须逐字完整；不得缺字、重复、错别字、乱码、残缺笔画、断裂字形、拉伸变形或被裁切。没有用户确认的文字时，画面不得出现可读的伪文字。
2. **文字排版逻辑**：同一信息层级使用一致的字体气质、字重、颜色和对齐逻辑；标题与副标题的阅读顺序清楚，行距、字距和换行自然；文字不与人物脸部、手势、关键道具或画面边缘发生无意碰撞。主标题仍为唯一第一视觉中心。
3. **人物五官与身份**：只要用户提供人物参考图，必须逐项对照脸型轮廓、额头与发际线、眉眼间距与眼型、鼻梁/鼻尖、嘴唇、下颌线、耳朵、肤色、发型、眼镜、胡须和服装等稳定特征；多视图/三视图必须全部纳入比对。人物身份不得被重绘为相似陌生人，不得美化到改变面部结构或五官比例，不得替换、删除或新增眼镜、胡须等身份特征。用户未授权时，不得修改人物的服装版型和关键外观。
4. **画面完整性**：检查手指、手臂、衣物、纸张、道具和透视是否自然；不得有多余肢体、破损物体、边缘融化、拼接痕迹、水印或 logo。

任一项不通过时，只针对失败项重新生成或修正，然后从第 1 项重新验收；最多迭代 3 次。第 3 次仍无法满足人物身份或文字准确性时，说明具体失败项，不交付为最终图。

### 第五步：验收

逐项检查：

1. 缩小到手机缩略图后，主标题是否仍然清楚？
2. 一秒钟内，能否知道它讲什么、有什么用、为什么可信？
3. 整张封面是否只有一个视觉中心？
4. 主标题、副标题和角标的层级是否清楚？
5. 可信标签是否真实，有没有夸大或伪造权威？
6. 是否存在可以删除而不影响理解的文字、图标、边框或装饰？
7. 文案是否与笔记正文一致，没有“封面承诺大于内容交付”？
8. 中文、数字、英文大小写和专有名词是否准确？
9. 是否按用户确认的风格和数量独立交付，多张时不是同一构图的轻微变体？
10. 是否已完成标题确认、打开风格站供选择、风格确认，并忠实执行最终选择？

任意一项不通过，先做减法，再重新验收。

## 默认输出格式

按 [references/output-template.md](references/output-template.md) 的格式输出。

需要更详细的标题、信息层级、视觉布局和可信证据规则时，读取 [references/cover-design-sop.md](references/cover-design-sop.md)。

## 迭代与经验回写

用户反馈某个结果不好时：

1. 先修改当前方案。
2. 判断反馈是否能抽象成以后都适用的规则。
3. 将可复用经验写成明确、可执行、可验收的句子，例如：
   - 不写“标题短一点”，改成“主标题默认不超过 16 个中文字符；超过时先压缩再生成”。
   - 不写“画面简单一点”，改成“封面只保留一个视觉中心；删除不影响理解的装饰元素”。
4. 只有在用户明确要求更新 Skill，且当前环境允许编辑 Skill 文件时，才修改本 Skill；否则输出建议新增的规则文本。

## 禁止事项

- 不编造官方背书、数据、测试结果、页数、身份或案例。
- 不以擅自改写已确认标题制造方案差异；差异应体现在所选风格与构图。
- 不把“一次生成 4 张”降级成只给 4 套文案、只生成 1 张，或输出一张四宫格。
- 不让主标题、副标题、角标和装饰同时抢夺注意力。
- 不为了追求“高级感”牺牲缩略图可读性。
- 不使用与正文无关的夸张承诺诱导点击。
- 不在信息已经足够时反复追问用户。

## 通用生图规则（适用于所有风格、所有出图）

以下规则对所有封面生成生效，不局限于某一个风格：

- **人物参考必加提示词**：只要提供了人物照片参考，每一次图像生成或编辑请求的最终 prompt 都必须原样加入：“严格保留本人真实脸型、五官比例与发型基础，不要过度美颜、不要欧美化、不要变成陌生人”。此规则覆盖所有风格、批量中的每一张、风格测试、局部修改与失败重试；不得仅写在方案说明中，不得用翻译或近义改写替代。调用生图工具前逐条检查 prompt 已包含该原句，并同时传入对应人物参考图。
- 画布比例强制为 3:4（宽:高 = 0.75）。实际像素使用 1080×1440 ；**禁止**输出 1024×1536（那是 2:3，画面会被纵向拉长变形），也禁止任何非 3:4 比例。所有 ImageGen 调用或设计工具导出均按 3:4 执行，不可拉长。
- 字体多样性：标题字体根据图像内容自动推荐，可混合粗黑体、手写体、变体/装饰体、竖排等，保留原参考图的手写体、变体风格特征。
- **人物控制图身份锁定**：只要用户提供人物控制图（尤其是包含正面、侧面和背面/多表情的三视图），必须先逐项识别并在生图 prompt 中明确锁定脸型轮廓、额头与发际线、眉眼间距与眼型、鼻梁/鼻尖、嘴唇、下颌线、耳朵、肤色、发型、眼镜、胡须和服装。成图验收时逐项同三视图对照；任一稳定特征明显漂移，即判定人物身份不通过，针对人物重新生成，不能将“相似陌生人”交付为结果。

## 风格学习工作流

让用户把喜欢的封面图「教」给 Skill，提取成可复用的风格模板，并登记到 references/styles/README.md。

### 风格文件分区（必须遵守）

    references/styles/
    ├── <style-id>.json                         # 风格规则
    ├── source-references/<style-id>/            # 用户上传的全部学习参考图
    ├── approved-examples/<风格中文名>.<ext>     # 用户确认通过后选择保留的测试示意图
    └── README.md                                # 索引、来源与示意图链接
    output/style-tests/<style-id>/               # 所有 Phase 3 测试图与迭代图

- 学习参考图与测试图必须分开保存；绝不把测试图混入 source-references/，也不把参考图当作测试结果或成品示意图。
- approved-examples/ 只保存用户明确确认过的测试图。文件名使用该风格的中文名称；若同名已存在，新增版本号，不覆盖旧图。
- output/style-tests/ 中的测试图保留作为测试记录，除非用户明确要求删除。

### 阶段门禁（不可跳过）

- 用户说“分析 / 学习 / 加入新风格”时，默认只进入 **Phase 1**；即使风格特征很明确，也不得直接写 JSON、复制参考图、更新索引或声称风格已保存。
- 必须先把分析以自然语言展示给用户，并等待其对分析和风格名称作出明确确认，才可进入 **Phase 2**。用户未命名时可以建议名称，但“采用该名称并保存”仍需用户确认。
- **Phase 2 完成保存后，必须单独询问是否进入 Phase 3**；不得把“学习风格”或“加入新风格”理解为已授权消耗生图资源进行测试。
- 用户若只要求检查、分析或修订既有风格，保持在相应阶段；明确说明当前进度与下一步所需确认，避免补做未获授权的生成。

### Phase 1：上传与分析

请用户上传 1–5 张参考图（支持 PNG / JPG / WebP）。

- 0 张：提示「至少需要 1 张参考图」。
- 超过 5 张：选取视觉差异最大的 5 张，并说明筛选原因。

用 Read 工具读取每张图片，分析以下维度：

- 配色：主色、辅色、点缀色（尽量给出 hex 值）、整体色彩情绪。
- 字体感：粗细（轻 / 常规 / 粗）、风格（无衬线 / 衬线 / 手写 / 装饰）、大小层级。
- 构图：文字与人物的位置关系、文字占比、留白多少、对齐方式。
- 装饰元素：有无边框、贴纸、图标、几何图形、背景纹理。
- 整体氛围：用 2–3 个关键词概括（如「温暖、治愈、ins 风」）。

若多张图风格冲突，明确指出冲突点，请用户选择方向，而不是取平均。

向用户展示分析结果（用自然语言，不要直接甩 JSON）：

> 我从你的图片中提取了这些风格特征：  
> 配色：暖奶油底色，深棕文字，粉色点缀  
> 字体：标题超大粗体，副标题细小，对比强烈  
> 构图：人物居中，标题压在头顶，底部色块横幅  
> 氛围：温暖、生活感、小红书爆款风  
> 你觉得这些对吗？有没有想调整的地方？

### Phase 2：确认与生成 Prompt

用户确认分析结果（允许最多 3 轮调整）：

- 「颜色再亮一点」→ 调整配色描述。
- 「想要更简约」→ 减少装饰元素。
- 「字体感觉不对」→ 进一步询问偏好。

请用户为这个风格起个名字（建议基于氛围关键词，如「暖橙励志」）。

只有用户明确确认“分析正确 / 按此保存”且确认风格名后，才生成 JSON、写入 references/styles/、把全部上传的学习参考图复制到 references/styles/source-references/<style-id>/ 并更新 README。若用户只确认部分特征，先按反馈更新分析，继续等待完整确认。此时不得写入或复制任何测试图到 references/styles/。

根据分析结果，生成对应的风格 prompt，格式与风格库 JSON 一致：

```json
{
  "name": "用户起的中文名",
  "prompt": "根据分析结果撰写的图片生成提示词..."
}
```

Prompt 撰写要点：

- 明确【文字区域划分】（主标题在哪、副标题在哪）。
- 明确【字体风格】（参考分析结果翻译成生图描述语言）。
- 明确【背景 / 场景】。
- 结尾加【禁止事项】（禁止多余文字、禁止修改人脸、禁止非 3:4、禁止标签与色块重叠）。
- 系列标签等可变文案：生成前必须强制以对话框形式询问用户，并注明「推荐 ip 名称」；不得写死固定文字（详见「通用生图规则」）。

把生成的 JSON 保存到 references/styles/ 目录，文件名用英文、与风格库已有风格格式一致，例如 references/styles/warm-orange-motivation.json（用 Write 工具写入）。

把全部学习参考图保存在 source-references/<style-id>/，保留原始文件名；JSON 的 source_references 字段使用这些相对路径。

更新 references/styles/README.md，登记风格名称、JSON 文件、全部学习参考图与 prompt 摘要；此阶段的“示意图”栏标为“待测试确认”，不得链接到测试图。

确认：「✅ 风格『{name}』的规则与学习参考图已保存。要不要用这个风格试生成一张看看效果？」

### Phase 3：测试生成

询问用户：「要不要用这个风格试生成一张看看效果？」

仅在用户明确回答同意后才调用任何生图工具或脚本；未同意时，交付已保存的风格文件与可选测试入口，不生成图片。

若用户同意，收集测试内容，要求用户提供文案，和人物控制图。每次生成与迭代都保存到 output/style-tests/<style-id>/，文件名标注主题与版本；不得保存进 references/styles/。

用 Read 工具展示生成结果，和原参考图放在一起让用户对比。

若不满意：找出哪个维度有偏差，回到 Phase 2 修改 prompt，最多迭代 3 次。

3 次后仍不满意：「可以先保存当前版本，之后随时再优化」。

测试满足要求后，等待用户明确确认“测试通过”或等效表述；随后单独询问：「测试已通过。是否将这张测试图作为『{name}』的风格样式示意图加入 styles？」

- 用户确认加入：把用户选定的测试图复制到 references/styles/approved-examples/<风格中文名>.<ext>，并在 README 的该风格行链接为“已确认示意图”。源测试图仍保留在 output/style-tests/<style-id>/。
- 用户拒绝加入或没有明确答复：不得把测试图复制到 references/styles/；只交付测试文件路径与后续可选操作。

### 错误处理

- 参考图分辨率过低（短边 < 500px）：提醒用户分析精度可能下降，建议换高清图。
- 参考图不是封面设计（纯照片、无排版元素）：说明此工作流需要有文字设计的封面图，纯人像照适合直接去生成封面。
- 用户中途放弃：告知当前进度已到哪一步，风格文件是否已保存，下次可以从哪里继续。
